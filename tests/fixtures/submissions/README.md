# Submission Fixtures

Store reusable source snippets and execution inputs for sandbox and solve-flow tests.

## Good fixture candidates

- accepted examples for Python, Rust, and C++;
- compile-error example;
- runtime-error example;
- timeout or unsupported-tool example.

## Keep them small

The point is fast diagnosis, not exhaustive language coverage in one file.
